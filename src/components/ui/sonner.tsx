import { Toaster as SonnerToaster, toast } from "sonner";

export const Toaster = SonnerToaster;

// Optionally re-export toast so you can import from "@/components/ui/sonner"
export { toast };

export default Toaster;
